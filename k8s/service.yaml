apiVersion: v1
kind: Service
metadata:
  name: kids-tales
  namespace: kids-tales
  labels:
    app.kubernetes.io/name: kids-tales
spec:
  selector:
    app: kids-tales
  ports:
    - name: http
      port: 80
      targetPort: 3000
      protocol: TCP
  type: ClusterIP
